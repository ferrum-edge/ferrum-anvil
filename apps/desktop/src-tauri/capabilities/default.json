{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities: core events/window basics, message/confirm dialogs and the open dialog (only to fill the Workload API token-file setting). No shell, no filesystem plugin, no HTTP plugin, no save dialog — file commands take grants from the backend's own native dialogs (file_choose), never a path from the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-message",
    "dialog:allow-ask"
  ]
}
